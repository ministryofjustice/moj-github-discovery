# 219: Dashboard and Dashboard_CLI.py Operational Restoration

## Introduction

In the early stages of the DevX GitHub Audit CLI’s development, prototype dashboard scripts were developed to visualise the findings of some of the scripts.  

As the requirements of the CLI evolved and more focus was placed on script enhancements, the dashboard scripts have been left untouched for a significant period. This document aims to show the current state of these scripts, what changes have been needed to make them operational once more, how they can be used in their current state, and the recommended approach to optimise them and incorporate the rest of the audit scripts.

### Dashboard.py Overview

`dashboard.py` is currently located under `scripts/` as a standalone script, not linked up to the `audit-cli` entrypoint.
It is currently executed by standard methods, passing a SQLite `.db` file as an optional argument via `--db`. Execution follows:

```shell
uv run python scripts/dashboard.py --db /path/to/database.db
```

At a script level, `dashboard.py` utilises [dash](https://dash.plotly.com/), a Python framework used to build interactive, data-driven web applications and dashboards, with the aim of enabling data scientists (or appropriate) to build full-stack web appls without any advanced frontend knowledge.

It achieves this by abstracting 3 core technologies into a single interface:

- **Flask:** Supplies the underlying backend web server functionality
- **React.js** Renders the user interface (UI) in the browser
- **Plotly.js** Generates interactive chargs, graphs, and maps

Upon execution, `dashboard.py` starts the web application on port `8095` at `localhost` and attempts to load a SQLite database file via the `--db` arg (defaults to `repo_audit.db` at the repo root if not provided). From here, the SQLite database's data can be analysed. Further details are provided in the [Usage](#dashboardpy-usage) section below. Currently it will only load data generated via `list_repos.py`

### Dashboard.py Usage

As either dashboard script requires some data to be present, a `.db` file needs to be generated by running the `audit-cli`. Using `list_repos`:

```shell
uv sync
uv run audit-cli --scripts list_repos
```

This will output the database file to `internal/repo_audit.db` by default if not altered in `config/audit.config`.
`dashboard.py` can now be executed to load this database:

```shell
uv run python scripts/dashboard.py --db internal/repo_audit.py
```

The flask web app can then be loaded (locally) via `http://localhost:8050`, users can then review the data presented per repository to identify compliance issues requiring resolution.

### Dashboard.py Current Capabilities

- Can only read `list_repos.py`-related data
- Displays the current total repositories count for the database
- Has search / filter by repo name
- Allows filtering only by repos with flags (but not flag-specific)
- Current display for dashboard follows the below structure:

| Repository | Status         | Language | Stars | Open Issues | Dependabot | Branch Protected | Flags                  |
| ---------- | -------------- | -------- | ----- | ----------- | ---------- | ---------------- | ---------------------- |
| repo       | Public/Private | Language | 123   | 123         | 123        | TRUE/FALSE       | OK / flag1, flag2, ... |

For a given repository, once selected from a row, the following details are provided under associated subheadings:

**Repository Info:**

| Field Name | Description                                      | Notes                                                |
| ---------- | ------------------------------------------------ | ---------------------------------------------------- |
| Name       | Repository Name                                  |                                                      |
| URL        | Repository URL                                   |                                                      |
| Private    | Repository visibility                            | False if repo is public, true if private or internal |
| Fork       | Is the repository a fork?                        | TRUE/FALSE                                           |
| License    | Does the repository have a license file present? | TRUE/FALSE                                           |

**Security Alerts:**

| Field Name      | Description                          |
| --------------- | ------------------------------------ |
| Dependabot      | Count of open dependabot issues      |
| Secret Scanning | Count of open secret scanning issues |
| Code Scanning   | Count of open secret scanning issues |

**Comunity Files:**

| Field Name      | Description                                        |
| --------------- | -------------------------------------------------- |
| Security Policy | Does the repository have a `SECURITY.md` file?     |
| Code of Conduct | Does the repository have a code of conduct file?   |
| Contributing    | Does the repository have a `CONTRIBUTING.md` file? |

**CI/CD:**

| Field Name  | Description                                                         |
| ----------- | ------------------------------------------------------------------- |
| Workflows   | Count of workflows present within the repository                    |
| Has Tests   | Does a workflow exist that focuses on tests (unit/integration/etc.) |
| Has Linting | Does the repo contain a  linting workflow?                          |

**Flags:**

Compliance issues are identified by **flags**, current examples are, as seen in [README.md](../README.md):

- `archived` - Repository is archived (no longer maintained)
- `fork` - Repository is a fork
- `no_license` - No license file present
- `public_unprotected_default_branch` - Public repo with unprotected default branch
- `dependabot_alerts_present` - Dependabot has found vulnerable dependencies
- `secret_alerts_present` - Secret scanning alerts exist
- `code_scanning_alerts_present` - Code scanning alerts exist
- `no_security_policy` - Missing SECURITY.md
- `no_code_of_conduct` - Missing CODE_OF_CONDUCT
- `no_actions_workflows` - No CI/CD workflows configured
- `no_detected_tests` - CI/CD exists but no test detection
- `no_detected_linting` - CI/CD exists but no lint detection

### Dashboard_CLI.py Overview

`dashboard_cli.py` is currently located under `scripts/` as a standalone script, not linked up to the `audit-cli` entrypoint.
It is currently executed by standard methods, passing a SQLite `.db` file as an optional argument via `--db`. Execution follows:

```shell
uv run python scripts/dashboard_cli.py --db /path/to/database.db
```

At a script level, `dashboard_cli.py` utilises Pandas Dataframes and loads the data from SQLite similar to `dashboard.py`

Upon execution, `dashboard.py` starts the web application on port `8095` at `localhost` and attempts to load a SQLite database file via the `--db` arg (defaults to `repo_audit.db` at the repo root if not provided). From here, the SQLite database's data can be analysed. Further details are provided in the [Usage](#dashboardpy-usage) section below. Currently it will only load data generated via `list_repos.py`

### Dashboard_CLI.py Usage

Similar to `dashboard.py`, a `.db` file needs to be generated by running the `audit-cli`. Using `list_repos`:

```shell
uv sync
uv run audit-cli --scripts list_repos
```

This will output the database file to `internal/repo_audit.db` by default if not altered in `config/audit.config`.
`dashboard.py` can now be executed to load this database:

```shell
uv run python scripts/dashboard_cli.py --db internal/repo_audit.py
```

This then prints out the dashboard similar to `dashboard.py` in the terminal. An example follows below:

```shell
Repository |  Status         | Language | Stars | Open Issues | Dependabot | Branch Protected | Flags 
ministryofjustice/laa-6dg-haproxy-errordocs Private              CSS      0            0           0        0          0         ✗ archived, no_license, no_security_policy, no_actio
ministryofjustice/laa-6dg-infrastructure Private            Jinja      0            3           0        0          0         ✗ archived, no_license, no_security_policy, no_code_
ministryofjustice/laa-OBIEE Private            Shell      1            4           0        0          0         ✓ no_license, no_security_policy, no_actions_workflo
```

### Dashboard_CLI.py Current Capabilities

- `dashboard_cli` is effectively the same as `dashboard`, however it's a one-time execution, there's no capability to:
  - Search
  - Filter
  - Review specific repositories

## Development Opportunities

- [x] Update default arguments to reference new `internal/` directory rather than repository root.
- [ ] Not all repositories bring up panels - `AttributeError: 'NoneType' object has no attribute 'get'` in `update_detail_panel` - possible issue with empty data
- [ ] PRIVATE = `True/False` -> Visibility: Public/Private/Internal, what's the sitch from repo details's return call if internal?
- [ ] Panels to be extended with info:
  - [ ] Default branch protection
    - [ ] Compliance method
    - [ ] GitHub-Community-related standards
- [ ] Flags need extending:
  - [ ] Unprotected default branch regardless of visibility
  - [ ] Branch protection compliance
    - [ ] Per-github-community standard flags?
- [ ] (Optional/Dependent) Does a "public" warning flag need noting if private-by-default becomes apparent.
- [ ] Filter by specific flags?
- [ ] How to close a panel - if a new panel's opened it should be front and center for the users
- [ ] Data loading performance is not optimal, and a 4500+ size DB times out during loading, how can performance for this be improved? Requires investigation (SQLite may not be the most appropriate)
- [ ] Currently only `list_repos`-related data is loadable, the dashboard pages would be required for the rest of the scripts
  - [ ] Spencer's work for the data model likely needs bringing back into the fore to understand the data model overlaps e.g. `archive_repos` shouldn't need to re-collect repo details
  - [ ] How would a dashboard look for say, `github_workflow`?
- [ ] Styling: We'd want this to align to standard MoJ page styling
- [ ] Dashboard tests will be required
- [ ] **Hypothetical:** What if the scripts were running continuously and updating a live DB, THEN the dashboard could perhaps be runnable locally and pointing to the database rather than have them need to run the script?
- [ ] Is the CLI version 100% necessary at all now? Flags are getting trimmed/cut as well in the output
